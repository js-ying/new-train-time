export enum PathEnum {
  trHome = "/",
  thsrHome = "/THSR",
  tymcHome = "/TYMC",
  busHome = "/bus",
  trSearch = "/search",
  thsrSearch = "/THSR/search",
  tymcSearch = "/TYMC/search",
  // 公車為單頁（搜尋 + 看板同頁），search 與 home 指向同一路徑
  busSearch = "/bus",
  trStation = "/station",
}
